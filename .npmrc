# Reactively uses pnpm exclusively.
engine-strict=true

# Keep the dependency graph honest: packages must declare what they import.
# Hoisting only what the toolchain genuinely needs to resolve at the root.
strict-peer-dependencies=false
auto-install-peers=true
