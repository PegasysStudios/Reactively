import { Hero } from "@/components/marketing/hero";

/**
 * Public landing page.
 *
 * A server component with no client JavaScript beyond what Next needs for navigation.
 */
export default function MarketingPage() {
  return <Hero />;
}
