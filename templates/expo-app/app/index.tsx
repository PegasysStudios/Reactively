import { HomeScreen } from "@/generated/screens/HomeScreen";

/**
 * REACTIVELY-OWNED FILE — regenerated from the project document. Do not edit.
 *
 * Route `/`. Route files re-export a generated screen so that renaming a screen changes
 * one import rather than moving a whole component between files.
 */
export default HomeScreen;
